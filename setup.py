from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='learning-mlops-project to understand dvc pipeline versioning using dvc local repo',
    author='anuj',
    license='',
)
